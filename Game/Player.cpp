#include "Player.h"

Player::Player() : Entity() {
    // we called superclass constructor so nothing more to see here
}

Player::Player(int h, int ap, std::string n) : Entity(h, ap, n) {
    // we called superclass constructor so nothing more to see here
}
