#include "Enemy.h"

Enemy::Enemy() : Entity() {
    // we called superclass constructor so nothing more to see here
}

Enemy::Enemy(int h, int ap, std::string n) : Entity(h, ap, n) {
    // we called superclass constructor so nothing more to see here
}
